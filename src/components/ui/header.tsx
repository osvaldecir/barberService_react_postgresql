import { Card, CardContent } from "@/components/ui/card";

const Header = () => {
  return (
    <Card>
      <CardContent>
        {/* Conteúdo do Header */}
      </CardContent>
    </Card>
  );
};

export default Header;